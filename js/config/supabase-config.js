/**
 * Optional static defaults for your Supabase project (Project Settings → API
 * in the Supabase dashboard). The anon/public key is safe to ship in
 * client-side code — it is meant to be exposed — as long as Row Level
 * Security policies on the tables restrict what it can actually do.
 *
 * You don't need to edit this file: Administração → Fonte de dados has a
 * form to set the same values from the browser (saved to localStorage,
 * which always takes priority over what's defined here). Filling these in
 * is only useful if you want the values checked into the repo instead of
 * typed in on every browser/device.
 */
export const SUPABASE_URL = "https://jhgzywafedwbfsbhaalr.supabase.co";
export const SUPABASE_ANON_KEY = "sb_publishable_T_MTGjKpk1xGYonBxNfgfw_VLxVMp5e";
